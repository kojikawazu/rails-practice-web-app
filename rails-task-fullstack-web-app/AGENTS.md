# Full-stack Rails instructions

親ディレクトリの `AGENTS.md` に加え、`rails-task-fullstack-web-app/**` を変更する前に `../.claude/rules/fullstack.md` を読んで守ってください。

`app/models/**` または `db/**` の変更には、さらに対象ディレクトリの `AGENTS.md` を適用します。
