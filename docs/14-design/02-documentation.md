# ドキュメント方針

## 目次

- [原則](#原則)
- [影響マップ（変更種別→更新必須ドキュメント）](#影響マップ変更種別更新必須ドキュメント)
- [完了条件（opt-out）](#完了条件opt-out)
- [修正忘れ注意](#修正忘れ注意)
- [ドキュメント配置](#ドキュメント配置)

[← 目次に戻る](README.md)

## 原則

- ドキュメントは**常に最新の状態を維持する**。コード変更時はドキュメントも必ず更新する。
- 仕様書とコードの乖離を防ぎ、**製造性（仕様書からコードへの再現性）を担保する**。
  - 仕様書を読めば実装方針が明確になるレベルを目指す。
- ルールの正本は `.claude/rules/` に置く。本設計方針（`docs/14-design/`）は HOW/WHY を補足し、仕様書（`docs/03`〜`docs/09`）は WHAT を定義する。役割の重複を避ける。

## 影響マップ（変更種別→更新必須ドキュメント）

「どのドキュメントだっけ？」を考えさせないための逆引き表（`.claude/rules/documentation.md` が正本）。

| 変更種別 | 更新必須ドキュメント |
|---|---|
| ルーティング・コントローラー・API レスポンス | `docs/07-api-specification.md` |
| モデル・マイグレーション・ActiveRecord スキーマ | `docs/05-data-specification.md` |
| 認証・認可・CORS・シークレット・セキュリティ対策 | `docs/06-security-specification.md` |
| 機能追加・変更、画面・View | `docs/03-functional-specification.md` |
| テスト（RSpec / request spec / system spec / FactoryBot） | `docs/08-test-specification.md` |
| 構成・ディレクトリ・Gem 追加/変更・依存関係 | `docs/09-architecture-specification.md` |
| 開発ルール・規約・ワークフロー変更 | `CLAUDE.md` / `.claude/rules/` |
| セットアップ・起動/実行手順・機能概要・技術スタック | `README.md` |

該当する変更がない場合はスキップする。

## 完了条件（opt-out）

- 変更は、影響マップの対応ドキュメントを**同一 PR 内で更新する**ことを完了条件とする。
- 更新不要と判断した場合は、**PR 説明にその理由を明記する**（省略＝未対応とみなす）。
- この乖離チェックは `/self-review` と `/pr-create` の確認対象に含まれる。

## 修正忘れ注意

- 以下のファイルはコード変更時に**修正漏れが発生しやすい**ため、特に注意する。
  - `/README.md` — ルート直下（セットアップ手順・技術スタック）
  - `/CLAUDE.md` — ルート直下（ルール・ワークフロー）
  - `/docs/09-architecture-specification.md` — 構成・依存変更の追従先
- PRレビュー（セルフレビュー含む）で、これらが更新されているかをチェック項目とする。

## ドキュメント配置

```
docs/
├── 01〜10-*.md          # 要求・要件・各種仕様書
├── 11-tasks.md          # タスク管理
├── 12-code-reading-guide/   # コードリーディング・ナレッジ
├── 13-mockups/          # モック画面（静的 HTML）
├── 14-design/           # 設計方針（本ディレクトリ）
└── 15-test-design/      # テスト設計
```
